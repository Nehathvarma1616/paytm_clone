const JWT_SECRET = "nehath";

module.exports = { JWT_SECRET };
// export the JWT_SECRET with object
// if u don't export with in object then it throws error as below
// Error: secretOrPrivateKey must have a value
